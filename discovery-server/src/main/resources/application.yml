server:
  port: 8761

spring:
  application:
    name: discovery-server

  profiles:
    active: dev

  output:
    ansi:
      enabled: always

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
