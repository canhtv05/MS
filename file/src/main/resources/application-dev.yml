logging:
  level:
    '[root]': WARN
    '[com.leaf]': INFO
    '[org.springframework]': WARN

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/file}

cloudinary:
  url: ${CLOUDINARY_URL}
